<div>
<br>
<div ng-show="editAddressMessage.length>0" class="alert alert-success" style="width:300px;height:50px;"><small>{{editAddressMessage}}</small></div>
<strong class="text-info">Update Your shipping address.</strong>
<form method="post" name="editAddressForm" ng-submit="changeAddress()">
<div class="form-group">
           
	            <textarea name="address" ng-minlength="10" class="form-control" rows="3" ng-model="currentUser.address" placeholder="Enter address"  required></textarea>
	            <small  ng-show="editAddressForm.address.$error.required && !editAddressForm.address.$pristine" class="text-danger">Address Required</small>
	             <small class="text-danger" ng-show="editAddressForm.address.$error.minlength && !editAddressForm.address.$pristine">Address must be 10 characters long</small>
	         
            
        </div>
        
<p class="text-info">{{currentUser.address}}</p><br>
<button class="btn btn-warning" type="submit" ng-disabled="editAddressForm.$invalid">Update Address</button>

</form>

</div>